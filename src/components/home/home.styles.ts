import styled from 'styled-components';

export const HomePageSectionContainer = styled.div`
    padding: 0px;
    border-bottom: unset;
    margin: 0px 20px;
`;

export const HomePageSectionHeaderContainer = styled.div`
    display: flex;
    -webkit-box-align: center;
    align-items: center;
    -webkit-box-pack: justify;
    justify-content: space-between;
    margin: 4px 20px 0px 0px;
`;

export const HomePageSectionBody = styled.div`

`;

export const HomePageSectionHeader = styled.div`
    color: var(--color-daintree);
    font-family: Heebo;
    font-size: 16px;
    font-weight: 500;
    letter-spacing: 0px;
    line-height: 24px;
    cursor: pointer;
    position: relative;
`